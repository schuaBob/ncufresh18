<script>
    var url = new URL(window.location.href);
    document.getElementsByName('name')[0].focus()
    $("input[name=id]").val(url.searchParams.get('id'))// = url.searchParams.get('id');
    $("input[name=id]").focus() ;
    
    $("input[name=name]").popover({
        content: "名字有誤" 
    })
    $("input[name=name]").popover('show') 
    $("input[name=checkpassword]").popover({
        content: "兩次輸入密碼不同" 
    })
    $("input[name=checkpassword]").popover('show') 
</script>